version: '3.1'

services:
  mysql:
    image: mysql:latest
    container_name: sql-performante
    environment:
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3307:3306"
    volumes:
      - E:\mysql-data:/var/lib/mysql
